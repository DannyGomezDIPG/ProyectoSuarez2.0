import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Desarrollo_aplicaciones',
  webDir: 'www'
};

export default config;
